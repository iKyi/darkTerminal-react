export type SetupDropPagePropsType = {
  children?: any;
};

const SetupDropPage: React.VFC<SetupDropPagePropsType> = ({ children }) => {
  // *************** RENDER *************** //
  return <>setup drop page</>;
};

export default SetupDropPage;
