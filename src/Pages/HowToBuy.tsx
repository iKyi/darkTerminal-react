import { Box } from "@mui/material";

export type HowToBuyPropsType = {
  children?: any;
};

const HowToBuy: React.VFC<HowToBuyPropsType> = ({ children }) => {
  // *************** RENDER *************** //
  return <Box>How to buuy page</Box>;
};

export default HowToBuy;
